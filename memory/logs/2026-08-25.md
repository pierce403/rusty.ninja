# 2026-08-25

## Repository initialization

- Task: create a public `rusty.ninja` repository and initialize agent
  communications using the Recurse-inspired workflow.
- Decision: keep product scope explicitly open rather than inventing requirements.
- Added canonical agent instructions, a bounded task queue, feature acceptance
  tracking, curated memory, staged learnings, dated logs, and a curator skill.
- Verification: curator validation, link checks, whitespace checks, clean commit,
  public GitHub visibility, remote branch parity, and final clean status.
- Next: define the project's audience, problem, and first milestone with the owner.

## Vite placeholder and Pages

- Task: serve a hello-world placeholder as a Vite app and build it on every push
  to `main` with GitHub Actions.
- Decision: use the repository Pages URL and Vite base `/rusty.ninja/`; no custom
  domain has been configured.
- Verification: `npm ci` and Vite 8.2.2 production build passed; local preview
  served the project base and hashed assets; Actions run `32861449656` built and
  deployed commit `f1d90a7`; Pages reports `build_type: workflow`, public access,
  and enforced HTTPS; the live HTML, CSS, and JavaScript returned HTTP 200.
- Browser note: the in-app browser was unavailable, so the live copy and asset
  responses were verified directly but visual browser rendering remains untested.

## Apex domain

- Task: serve the Pages deployment from `https://rusty.ninja/` rather than the
  default project URL.
- Initial evidence: apex DNS resolves to GitHub Pages addresses, but the Pages API
  had no `cname` and HTTPS presented a certificate for the wrong hostname.
- Decision: configure only the requested apex domain and change Vite's base from
  `/rusty.ninja/` to `/`; do not configure `www`.
- Verification: Actions run `32862025021` deployed commit `66db8b6`; Pages reports
  `cname: rusty.ninja`, `build_type: workflow`, an approved certificate, and
  enforced HTTPS; HTTP redirects to HTTPS; the apex HTML and both root-relative
  hashed Vite assets return successful responses.

## Adaptive security wargame v1

- Task: replace the placeholder with the owner-defined endless Rust security
  training PWA while preserving the established agent workflow and Pages setup.
- Implemented 26 procedural generators, deterministic share links, an adaptive
  uncertainty-aware rating, exact resumable local state, durable replay
  protection, stats/settings, six original Rusty states, PWA assets, and mobile
  accessibility behavior.
- Review evidence: two independent red-team passes found and prompted fixes for
  answer ambiguity, unsafe/context classification, cancellation and lock fixes,
  exact resume, replay farming, import validation, keyboard behavior, modal
  feedback, icon transparency, and asset caching.
- Local verification: 49 tests pass; TypeScript, Vite production build, PWA
  artifact checks, and whitespace checks pass. The service-worker precache is
  39 entries / 773.90 KiB.
- Next: deploy the merged history, verify Actions and the apex application, then
  mark the feature and task complete with exact evidence.

## Adaptive security wargame deployment

- Resolved the unrelated local and remote histories without force, preserving the
  established Pages/domain/agent records while replacing the placeholder app and
  duplicate deployment workflow with the production wargame tree.
- Published commit `9bc8741`; Actions run `32868659617` passed dependency install,
  49 tests, production build, artifact upload, and the Pages deployment.
- Production verification at `https://rusty.ninja/` covered immediate answer and
  rating feedback, next-challenge generation, deterministic seeded reproduction,
  exact reload resume, stats/settings, and live manifest, service worker, and OG
  image responses. The manifest and service worker returned their correct content
  types and the service worker contains the expected precache/navigation routes.
- Remaining acceptance: exercise standalone installation, offline relaunch, and a
  narrow viewport in a mobile-capable browser. The available cloud browser reports
  no service-worker support and does not expose viewport emulation, so those checks
  were not claimed from this session.

## Contextual official documentation

- Added a template-specific official reading list to incorrect-answer feedback
  without changing generated challenge data or deterministic share reproduction.
- Every registered template maps to one to three relevant Rust, Serde, Tokio, or
  serde_json references; all 39 unique URLs returned HTTP 200 during verification.
- The links use large mobile tap targets, visible keyboard focus, descriptive source
  and topic labels, and safe new-tab attributes. Correct answers remain uncluttered.
- Verification: 53 tests pass, including full registry coverage and official-host
  constraints; TypeScript, production build, PWA artifacts, and whitespace pass.

## Opening syntax and vocabulary track

- Added six deterministic level 0.0–2.35 generators for bindings and shadowing,
  shared and mutable references, slices, explicit lifetimes, and `Option`/`Result`
  propagation with `?`.
- Added a typed `syntax-vocabulary` track, a non-finding Rust-language-behavior
  classification, an opening-session sampling preference, and a compact mobile
  orientation card. Existing canonical and pre-R1 short share links remain stable.
- Every new template links missed answers to primary Rust documentation; the 32
  registered templates now expose 50 references across 47 unique official URLs.
- Local verification: 57 tests pass; TypeScript, production build, PWA artifacts,
  deterministic branch review, official-link review, and whitespace checks pass.
- Production verification: commit `7c8857b` fast-forwarded `main`; Actions run
  `32905705468` passed its build and Pages jobs. The live custom domain served
  the new hashed bundle and reproduced a canonical shared-reference challenge
  with the opening-track card, immediate language-behavior grading, official
  Rust Book feedback after a miss, an enabled next action, and no horizontal
  overflow at the available browser viewport.

## Practical code-reading track

- Added eight deterministic generators spanning levels 0.35–5.25. Five ask for
  exact printed output and three ask for the best description of practical Rust
  involving control flow, `Vec::retain`, string normalization, fallible parsing,
  lazy iterators, `HashMap::entry`, clone-and-sort ownership, and `Drop` scopes.
- Added dedicated `code-comprehension` and `output-prediction` interactions plus
  a compact reading-track orientation card. Each challenge has four distinct,
  realistic answers and uses the non-finding Rust-language-behavior class.
- Added official Rust documentation for every new template and golden
  compatibility fingerprints for all 32 earlier canonical generators plus three
  historical short seeds.
- Next: complete the full local validation matrix, publish `main`, and record the
  Actions and production smoke evidence.
